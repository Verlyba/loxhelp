# Ralph Loop – checklist implementace LMS (dle PRD.md)
# Pravidlo: vždy JEDEN nejvyšší nezaškrtnutý úkol. Po dokončení: tsc+eslint, commit, [x] + poznámka.

[x] Analýza databázového schématu a návrh relací podle PRD.md.
    Poznámka (iterace 1, commit 92602d4):
    - Stávající schéma pokrývá: User/Class/Subject/SubjectPage/SubjectFile/Enrollment/
      StudyGroup+Member/Pair+Member/Assignment/Submission(unit-keyed)/Grade(+lock+extension)/Announcement.
    - Chybějící dle PRD doplněno: model AuditLog (§5C), model AssignmentConsent (§5B),
      SubjectFile.isPublished (§5A draft/published materiálů), Assignment.requiresConsent + consentText.
    - db push + generate hotovo, tsc/eslint čisté.
    - Zároveň dokončen jednotný dialogový systém (§4): DialogProvider (confirm/prompt) + sonner toasty,
      všechny nativní confirm/alert/prompt nahrazeny.
    - POZOR pro příště: DB se spravuje přes `bunx prisma db push` (ne migrate – v neinteraktivním
      shellu selže). Migrace v prisma/migrations/ jsou z rané fáze a NEodpovídají aktuálnímu schématu –
      před produkcí bude potřeba `migrate diff` reconcile. Data v dev.db jsou reálná → NIKDY migrate reset.

[x] AuditLog – zapojení (§5C).
    Hotovo (iterace 2, commit bb62fc0):
    - writeAudit(tx, actorId, entry) helper (Tx = typ z db.$transaction).
    - updateSubmissionState v jedné transakci porovná existing vs next a zapíše řádek pro každou
      změněnou dimenzi: GRADE_SET/CHANGE/DELETE, FEEDBACK_CHANGE, SUBMISSION_LOCK/UNLOCK,
      EXTENSION_SET/CLEAR. entityId=`${assignmentId}:${userId}`, targetName=jméno, old→new.
    - Data fn getGradeAudit (staff-only, entityType="Grade", entityId in unit) + typ AuditEntryView.
    - UI: rozbalovací sekce "Historie hodnocení (audit)" v GradingModalu (react-query, refreshKey=grade).
    - Pozn.: karta žáka zatím audit nezobrazuje (stačí GradingModal); lze doplnit později.
      SpeedGrader-lite náhled je už hotový (paralelní práce) — viz poslední bod.

[x] Draft/published materiálů (§5A).
    Hotovo (iterace 3, commit 1ac93e8):
    - getSubjectPage: pro studenty `files: { where: { isPublished: true } }`, staff vidí vše.
      SubjectFileItem rozšířen o isPublished.
    - setCourseFilePublished (staff-only) toggle akce.
    - UI: FileEditRow má Eye/EyeOff přepínač + odznak "Koncept"; FileGrid ukazuje odznak "Koncept"
      u nezveřejněných (staff je na stránce vidí). Nové uploady jsou default published (schema default true).
    - Pozn.: úkoly (Assignment.isPublished) i testy budou mít stejný pattern; testy zatím neexistují.

[x] Evidence souhlasu studenta (§5B).
    Hotovo (iterace 4, commit bc927cf):
    - Server: akce `recordConsent` ukládá `AssignmentConsent` se zmrazeným zněním textu a vybranou variantou.
    - Gate: v `uploadSubmission` je backendová kontrola bránící odevzdání bez souhlasu.
    - UI úkolu (Create/Edit): přidány přepínače a textarea pro text souhlasu v obou modalech.
    - UI studenta: pokud souhlas chybí, odevzdávání se skryje a zobrazí se `ConsentCard` (s možností zadat variantu a odeslat souhlas). Po udělení se odemkne nahrávání a ukáže se zelený info banner.
    - UI učitele: u každého člena jednotky (včetně dvojic/skupin) svítí ✓/✗ indikátor souhlasu a dole na stránce je tabulka „Evidence digitálních souhlasů“ pro právní krytí.

[x] Modul Testy – Správa testů (§2).
    Hotovo (iterace 5, commit [next]):
    - Schéma v DB pushnuto: Test, Question, QuestionOption, TestAttempt, Answer modely.
    - CRUD mutace a data loaders na backendu (test-actions, test-data) s RBAC kontrolami a skrýváním správných voleb pro studenty před odevzdáním.
    - Odpočet času na frontendu studenta s automatickým odevzdáním po vypršení limitu.
    - Učitelé mají editor otázek, nastavení a rozhraní pro ruční hodnocení otevřených textových otázek s celkovým skóre a zpětnou vazbou.
    - Ověřeno přes `bunx tsc`, `bunx eslint .` a `bun run build`.

[ ] Import studentů z matrik CSV/XLSX (§5D).
    Rozšířit stávající hromadný import o parser Bakaláři/ŠkolaOnLine exportu; celý zápis v transakci
    (selže jeden → nepropíše se nic). Náhled před importem + report chyb.

[ ] Transakce pro zbývající hromadné operace (§5D).
    generatePairsInGroupCore a createPair obalit do db.$transaction.

[ ] Dedikované karty s prokliky (§3, §4).
    Karta třídy (/classes/$id) a karta skupiny – detailní přehled + klikatelné vazby
    (z karty žáka → třída/skupina, z třídy → žáci/předměty). Vizualizace propojení.

[ ] Mobilní optimalizace studentských pohledů (§4).
    Audit responzivity: kurz, detail úkolu, odevzdávárna, známky, karty. Studenti = primárně mobil.

[ ] SpeedGrader-lite – inline náhled souborů (roadmapa).
    Náhled PDF/obrázku/textu přímo v GradingModalu (blob URL), ať učitel nemusí stahovat.
